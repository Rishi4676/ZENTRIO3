# Environment variables - NEVER commit these
.env
.env.local
.env.production
.env.txt

# Node modules
node_modules/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# JSON databases (local data - auto-generated, not needed in repo)
users_db.json
otp_db.json
reset_db.json
audit_logs.json

# Ignore built directories since Vercel now builds them dynamically
admin0/dist/
portal-login/dist/

# Firebase Admin SDK service account keys - NEVER commit these
*-firebase-adminsdk-*.json

# Payment & data backup files - contain sensitive records
*_db_backup.json
payments_db_backup.json
users_db_backup.json

# Scratch directories
scratch/

